projection implementation in class ZBP_C_ATRAV_FMB unique;
strict ( 2 );
use draft;
define behavior for ZC_ATRAV_FMB alias Travel
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}